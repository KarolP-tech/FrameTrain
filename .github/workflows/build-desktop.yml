# DEPRECATED - Use release-desktop-app.yml instead
# This workflow is kept for reference but disabled

name: Build Desktop App (Deprecated)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use release-desktop-app.yml instead'
        required: false

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "⚠️ This workflow is deprecated"
          echo "Please use: release-desktop-app.yml"
          echo "Trigger it by pushing a tag: git push origin v1.0.0"
